---
version: "3"
services:
  webcall:
    image: webcall
    build:
      context: .
    container_name: webcall_server
    volumes:
      - ./db/:/app/db/
    ports:
      - 127.0.0.1:8067:8067
      - 127.0.0.1:8071:8071
    restart: always
